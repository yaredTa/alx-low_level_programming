#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * add - adds two integers and returns the result
 * return: the sum of two integers
 */
int add(int n, int m)
{
int p;
p = n + m;
return (p);
}
